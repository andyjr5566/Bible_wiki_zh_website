# 聖經研讀知識庫 - 段落收合規則設定檔 (Collapse Rules)
# 
# 📝 使用說明：
# 1. 每一行代表一個你希望在網頁上「預設收合」的標題名稱。
# 2. 單層收合：直接寫標題名稱（例如：本章知識節點）。
# 3. 雙層收合：如果希望大標題點開後，旗下的各卷書（H3 標題）也能各自獨立收合，
#    請在該行後方加上：-> subheadings（例如：按書卷累積 -> subheadings）。
# 4. 井字號（#）開頭的行為註解，不會生效。
# 5. 修改此檔案後，重新發布或同步即可全站生效！

# ── 單層收合段落 ──────────────────────────────
本章知識節點
來源依據

# ── 雙層收合段落（大標題收合 + 各卷書獨立收合） ──
按書卷累積 -> subheadings
